name: dev-wyrm
services:
  web:
    build: .
    volumes:
      - .:/app
  celery_worker:
    build: .
    volumes:
      - .:/app
  celery_beat:
    build: .
    volumes:
      - .:/app
  flower:
    build: .
    volumes:
      - .:/app
  dev-tools:
    build: dev-tools
    env_file: .env
    volumes:
      - /app/dev-tools/
      - .:/app
    profiles:
      - tools
